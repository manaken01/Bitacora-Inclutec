import { Injectable } from "@angular/core";

/**
 * Common interfaces of the project
 */
/**
 * Interface to large notification
 */
export interface MessageData {
  message: string;
  title: string;
  type: string;
}
